server:
  port: 9001

spring:
  application:
    name: user-service

eureka:
  client:
    register-with-eureka: true
    # 외부에서 검색 가능한 형태
    fetch-registry: true
    # 유레카 서버 위치
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
